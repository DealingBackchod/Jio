.jio-header { background: #0057b8; padding: 10px; display: flex; align-items: center; }
.nav-menu li { color: #fff; margin: 0 12px; font-weight: 500; }
.hero { background: #f2f7fb; padding: 20px; text-align: center; }
.form-container { display: inline-flex; border: 1px solid #ccc; border-radius: 6px; overflow: hidden; }
.form-container input { border: none; padding: 10px; width: 150px; }
.form-container button { background: #0057b8; color: #fff; padding: 0 20px; }
.chip { border-radius: 20px; padding: 8px 16px; border: 1px solid #0057b8; margin-right: 8px; cursor: pointer; }
.chip.active { background: #0057b8; color: white; }
.plan-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px,1fr)); gap: 12px; }
.plan-card { background: #fff; border: 1px solid #e0e0e0; border-radius: 8px; padding: 16px; text-align: center; }
.plan-card .price { font-size: 1.5rem; font-weight: bold; color: #0057b8; }
